apiVersion: prodready.cluster/v1alpha1
kind: ClusterClaim
metadata:
  name: crossplane-eks
  labels:
    cluster-owner: cp 
spec:
  id: crossplane-eks
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    nodeSize: small
    minNodeCount: 1
